import { createStyles } from 'antd-style';

export default createStyles(() => ({
  searchContent: {
    display: 'flex',
    justifyContent: 'space-between',
  },
  searchOperation: {
    marginLeft: 'auto',
  },
  statusOption: {
    width: 128,
  },
  searchBtn: {
    margin: '0 8px 0 0',
  },
}));
